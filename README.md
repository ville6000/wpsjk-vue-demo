# WPSJK Vue Demo

## Requirements
* `node`
* `yarn` or `npm`

## Build Setup
### install dependencies
`yarn install`

### Serve with hot reload at localhost:8080
`yarn serve`

### Build for production with minification
yarn build

### Lint
`yarn lint`
